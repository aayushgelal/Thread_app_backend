import { mutation } from "./mutation";
import { queries } from "./queries";
import { resolvers } from "./resolvers";
import { typeDefs } from "./typedef";
const User = { mutation, queries, resolvers, typeDefs };
export default User;
